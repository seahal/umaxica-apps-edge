---
/*
 * The news frame's landing page. Copy (ja + en) lives here, as it did in
 * `src/routes/index.tsx`. `src/i18n.ts` holds only the shared shell vocabulary.
 */
import PageHero from '../../components/PageHero.astro';
import Base from '../../layouts/Base.astro';
import { type Locale, type PageCopy } from '../../i18n';
import { brandTitle } from '../../lib/title';

const COPY: Record<Locale, PageCopy> = {
  ja: {
    siteName: 'UMAXICA ニュース',
    title: 'News',
    heading: '最新のお知らせをお届けします',
    description: 'News and announcements from UMAXICA.',
    paragraphs: [
      '新機能のリリース、メンテナンスの予定、障害発生時のご報告などを掲載しています。重要なお知らせは順次このページに追加されます。',
    ],
  },
  en: {
    siteName: 'UMAXICA News',
    title: 'News',
    heading: 'The latest announcements',
    description: 'News and announcements from UMAXICA.',
    paragraphs: [
      'Feature releases, scheduled maintenance windows, and incident reports are published here. Important announcements are added to this page as they arise.',
    ],
  },
};

const lang = 'ja' satisfies Locale;
const copy = COPY[lang];
---
<Base locale={lang} title={brandTitle(copy.title)} description={copy.description} pagePath="/">
  <PageHero siteName={copy.siteName} title={copy.heading} paragraphs={copy.paragraphs} />
</Base>
