# Words this repository cannot spell any other way.
#
# Everything here is a NAME or a deliberate non-word. A real misspelling is
# fixed in the source instead. This checker's first finding was the product
# name misspelt with the third letter dropped, in seven apex test fixtures;
# there is no entry for it here because all seven were corrected.
#
# British spellings ("behaviour", "artefacts", "initialisation") are NOT here:
# they are correct English and are covered by `language: en,en-GB` in
# cspell.config.yaml.
#
# Keep the headings. A word added without one is a word nobody can review.

# --- The product, and the people who appear in the history ---
umaxica
mslo
Junio
Hamano
umaxicaappsglobaldc  # Compose project name Podman derives from the sibling
                     # Global/Rails repository's directory name, quoted
                     # verbatim from an observed tunnel-connector conflict

# --- Cloudflare and the Workers runtime ---
workerd
healthz
ipcity      # the `cf-ipcity` request header
NOERROR     # DNS response codes, in the tunnel operations runbook
NXDOMAIN

# --- The toolchain, by package or binary name ---
nuqs        # named in plans/ only; the catalog entry it pinned is gone
Pulumi      # named in docs/operations/ only to record that it is NOT used
tsgolint
hurlfmt
syncpackrc
smol        # smol-toml, an advisory floor in pnpm-workspace.yaml
endoflife   # endoflife.date
jscoverage
GHSA        # GitHub advisory identifiers
qqqr        # ...and a fragment of one: GHSA-8988-4f7v-96qf's neighbour

# --- Containers, Linux and the shell ---
tmpfs
btmpfs
userns
tzdata
nocopy
NOPASSWD
ISIG
SHLVL
infocmp
nohup
ncdu
entr
wscript
gsub
Ghostty

# --- Tailscale, for the opt-in remote-SSH overlay. Product and protocol names
# --- quoted from tailnet configuration and the tailscale/tailscale image.
tailnet
tskey
netavark
DERP

# --- Chromium's shared-library dependencies, listed in Containerfile so
# --- Playwright can run. These are package names, not words.
libasound
libatk
libatspi
libdbus
libgbm
libnspr
libnss
libxcomposite
libxdamage
libxfixes
libxi
libxkbcommon
libxrandr
libxrender
libxtst

# --- Typography: font families and OpenType feature tags ---
Hiragino
Kaku
Meiryo
Noto
chws        # the OpenType tag for contextual half-width spacing

# --- Dev container features and editor extension publishers, quoted verbatim
# --- from .devcontainer/ and .vscode/ identifiers.
aiassistant
bradlc
csjones
dbaeumer
eamodio
joedmck
jsburckhardt
nicolo
ribaudo
herdr
dcup
nolanjx
sliekens
srzstephen

# --- Vocabulary this repository's prose uses consistently. Each is a coined
# --- prefix form ("mis-", "un-") or an established abbreviation; none is a
# --- misspelling of an existing word.
misbinding
misbound
misconnection
misrouting
unrouted
unstub
unconfigured
unexercisable
unhashed
unmigrated
uncacheable
greppable
hrefs
jsons
sess
creds
ecom
changeme
blkd        # the BLOCKED status abbreviation in tools/verify-edge-connectivity.mjs
SPOF
ERRO
PKCE        # the OAuth Proof Key for Code Exchange extension, RFC 7636

# --- Deliberate non-words inside test fixtures. Each is a fragment of an
# --- encoded or intentionally malformed string, so there is no correct
# --- spelling to move it towards.
apiv            # from the route '/apiv0-lookalike'
eevil           # from the percent-encoded host 'jp%2eevil%2ecom'
Ffoo            # from the percent-encoded path '%2Ffoo.png'
vpcc            # a mistyped CLI argument, asserted to exit non-zero
Bāshettihalli   # a real place name, used as a cf-ipcity value to be stripped

# --- Security standards cited in the header and rate-limit rationales ---
ASVS        # OWASP Application Security Verification Standard
HSTS        # RFC 6797 HTTP Strict Transport Security, set in every _headers
